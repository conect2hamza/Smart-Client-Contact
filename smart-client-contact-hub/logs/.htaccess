# Block all direct access to this directory, regardless of Apache version.

# Apache 2.4+
<IfModule mod_authz_core.c>
	Require all denied
</IfModule>

# Apache 2.2 and earlier (or 2.4 with mod_access_compat)
<IfModule !mod_authz_core.c>
	Order allow,deny
	Deny from all
</IfModule>
